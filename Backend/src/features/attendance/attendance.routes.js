import { Router } from "express";

const router = Router();

// TODO: GET /api/attendance?employee=:id
// TODO: POST /api/attendance
// TODO: PUT /api/attendance/:id (manual correction, restricted)

export default router;
